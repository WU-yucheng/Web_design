2022/07/28
(# mean the wrods were recored last class)
flower

html:
# the 'section' in 'main':
#     '#news':
#         add 'h2', 'div'
#         the 'div' content 'figure', 'h2', and an 'a' in 'p'
#        use 'lorem' to generate fake words to fill the page

in 'section' '#service', use short key to add element:
    div.itme$*4>div>p{SERVICE TITLE}+a[href="#"]
    the '$' will add index after class name
    the '{}' use to add content
    the '[]' use to add the attribute

css:
# a way to import google font '@import' need to write after '@charset'
# '@font-face' can use costum 'font-family', but need to upload the font file
# set 'img' width always 100%
# the 'section' in 'main':
#     '#banner' give the 'display: flex', and 'justify-content: space-between' to separate 'header' and 'figure'
#     add 'h2' & 'h3' style

#     '#news' give a class 'title' for 'h2'

'font: inherit' will inherit father element's 'font'
'section' add 'display: flex' and set 'flex-direction: column' & 'align-items: center'
because 'section' become 'flex-direction: column', '#banner' need to set 'flex-direction: row'
add 'box_lst' & 'box_lst article' to set the 'article' in '#news'
add 'box-shadow', 'overflow: hidden' & 'border-radius' to 'box_lst article', let the words and images fit in the 'article'
the image can use 'overflow: hidden' to cut the area overflow
the 'img' in '#news figure' give 'object-fit: cover' to set the image align to center, it must to write 'overflow: hidden' in its father
edit the style of 'h2', 'p', and 'a'
!!! '#news a' use 'display: -webkit-box', it a experimental function by browse, it is not CSS standard yet